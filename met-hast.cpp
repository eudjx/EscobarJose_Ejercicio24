#include <iostream>
#include <cmath>
#include <random>
#include <stdlib.h>

int main(){
  float sigma=0.0;
  float N=100000;
  float delta=1.0;
  float p=0;
 
  gauss=exp(-x**2/(2.0*sigma**2))/np.sqrt(2.0*M_PI*sigma**2)
  srand48(8);
  
  for (i=1; i<1000; i++){
	  propuesta=(drand48()-1)+(drand48()-0.5)*delta
	  r=min(1, gauss(propuesta, sigma)/gauss(drand48()-1, sigma))
	  alpha=drand48()
	  if (alpha < r){
		  p=propuesta
	  }
	  else{
		  p=p
	  }
  }
	return 0
  }

